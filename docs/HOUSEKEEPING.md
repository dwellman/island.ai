# Housekeeping Notes

- Docker Compose auto-start is disabled by default so the CLI demo runs cleanly without a compose file. Future cleanup: remove the Docker integration entirely or reintroduce it behind explicit profiles and documentation once a compose setup exists.
